/**
 * Fichier destiné à contenir toutes les constantes du projet 
 */

/**
 * Définit l'adresse de l'API rest, afin de ne la modifier qu'à un seul endroit si on change de serveur
 */
export const environment: any = {
    backendUrl: 'http://localhost:8080'
}
